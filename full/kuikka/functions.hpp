/*
 * Tässä tiedostossa määritetään kaikki käytettävät funktiot
 */

// Käytettävä funktion alkunimi
// kuikka_fnc_funktio
class kuikka {
    class kuikka {
        // Määritä kansio, missä funktiot löytyvät
        // Myös esim. "oma/testi" on sallittava kansio osoite
        file = "kuikka";

        // Määritä skripti tiedostot
        // Tiedoston tulisi olla tässä tapauksessa fn_groupSpawn.sqf
        // Ajetaan funktiona kuikka_fnc_groupSpawn
        class addMenuAction;
        class addOnInventoryChanged;
        class groupSpawn;
        class lightSwitch;
        class mortar;
        class moveObjects;
        class paraJump;
        class progressBar;
        class randomInArea;
        class removeOnInventoryChanged;
    };
};
